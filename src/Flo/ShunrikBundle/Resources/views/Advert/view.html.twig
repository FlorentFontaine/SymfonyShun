{% extends "FloShunrikBundle::layout.html.twig" %}

{% block title %}
  Lecture d'une annonce - {{ parent() }}
{% endblock %}

{% block floshunrik_body %}
  <h2>{{ voice.title }}</h2>
  <i>Par {{ voice.author }}, le {{ voice.date|date('d/m/Y') }}</i>

  <div class="well">
  </div>
 
  <p>
    <a href="{{ path('flo_shunrik_homepage') }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
    <a href="{{ path('flo_shunrik_edit', {'id': voice.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-edit"></i>
      Modifier l'annonce
    </a>
    <a href="{{ path('flo_shunrik_delete', {'id': voice.id}) }}" class="btn btn-danger">
      <i class="glyphicon glyphicon-trash"></i>
      Supprimer l'annonce
    </a>
  </p>
{% endblock %}